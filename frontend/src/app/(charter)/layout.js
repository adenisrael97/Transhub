export const metadata = {
  title: "Charter a Vehicle",
  description: "Charter buses, SUVs, or trucks for group travel, corporate trips, events, or bulk delivery across Nigeria.",
};

export default function CharterLayout({ children }) {
  return children;
}
