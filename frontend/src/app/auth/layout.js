export const metadata = {
  title: "Sign In",
  description: "Log in or create your TransHub account to book seats, send goods, and charter vehicles.",
};

export default function AuthLayout({ children }) {
  return children;
}
