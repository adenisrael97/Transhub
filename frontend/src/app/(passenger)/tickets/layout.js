export const metadata = {
  title: "My Tickets",
  description: "View your TransHub e-tickets. Purchased tickets stay available offline so you can board even with no signal.",
};

export default function TicketsLayout({ children }) {
  return children;
}
