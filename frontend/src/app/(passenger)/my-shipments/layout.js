export const metadata = {
  title: "My Shipments",
  description: "Track all your sent parcels, review admin quotes, and pay for shipping — all in one place.",
};

export default function MyShipmentsLayout({ children }) {
  return children;
}
